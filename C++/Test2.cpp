#include <iostream>
using namespace std;
int main() {
    int w, i;
    cin >> w;
    i = (w / 2) * 2; if (w == w / 2 * 2)
    if (w == i) {
        cout << "YES";
    }
    else {
        cout << "NO";
    }
}